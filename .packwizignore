# .packwizignore follows the same format as .gitignore, and can be used to exclude unwanted files from pack exports
# See https://git-scm.com/docs/gitignore

# Exclude README
README.md

# Exclude packwiz binary
packwiz

# Exclude exported zip files
*.zip

# Exclude backups
*.bak

# Exclude Git folder, .gitattributes so that these are not tracked in pack index
.git/**
.gitattributes

# Exclude the client/ folder as this is configured per instance
client/**
